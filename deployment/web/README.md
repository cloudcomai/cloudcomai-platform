# Web deployment

The web application builds to static files in `apps/web/dist`. Production deployment must upload only the validated build output and must not require a Node.js production runtime.

Production deployment is not enabled in the foundation milestone.

