# Chat core

Transport-independent message state and synchronization logic will live here. Phase 1 will use incremental polling behind a transport boundary so WebSockets can be added later without coupling UI components to the transport.

