# Source repositories

The foundation reuses code from these repositories without modifying them:

| Target | Source repository | Source `main` commit |
| --- | --- | --- |
| `apps/web` | `cloudcomai/cloudcomai_chat_ui` | `1b97f8a` |
| `backend` | `cloudcomai/cloudcomai-backend` | `9d46d62` |

Future synchronization must be intentional and reviewed. This monorepo becomes the implementation location after the foundation PR is approved; it does not rewrite the history of either source repository.

