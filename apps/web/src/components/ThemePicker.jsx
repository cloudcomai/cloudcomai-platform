import React from 'react';
import { APP_THEMES } from '../services/appTheme';
export default function ThemePicker({value,onChange,required=false}) { return <div className={required?'theme-picker theme-picker-required':'theme-picker'}><h3>{required?'Choose your CloudComAI theme':'App Theme'}</h3><p>{required?'Select a theme to continue. Modern Blue is the default recommendation.':'Change the app color theme. Only the approved palette colors are used.'}</p><div className="theme-options">{Object.values(APP_THEMES).map(theme=><button type="button" key={theme.id} className={value===theme.id?'theme-option selected':'theme-option'} onClick={()=>onChange(theme.id)}><span className="theme-swatches">{Object.values(theme.colors).map(color=><i key={color} style={{backgroundColor:color}} />)}</span><strong>{theme.label}{theme.id==='modern-blue'?' (Default)':''}</strong></button>)}</div></div>; }
